// This file is required by Next.js App Router when using catch-all routes ([page])
// alongside other named routes (products, cart, etc.) in the same segment.
// Without it, parallel route resolution shows a warning.
export { default } from './page';
